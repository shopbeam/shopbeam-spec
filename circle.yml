# This is the circleci config file which is responsible for deployment of the spock repo
# to both heroku staging and production

machine:
  environment:
    BROWSERS: chrome
    SPAWNSELENIUM: true
  node:
    version: 0.10.26
general:
  artifacts:
    - "screenshots"
